<h1>Lista de usuarios</h1>
<h3> El usuario ha sido elimado con exito </h3>
	
<table>
	<tr>
		<th>Nombre del usario</th>
		<th>Email</th>
		<th>Acciones</th>
	</tr>
	<% @clients.each do |client| %>
		<tr>
			<td><%= client.name %></td>
			<td><%= client.email %></td>
			<td><%= link_to 'Ver Detalle',  client %> |
			 <%= link_to 'Editar', edit_client_path(client) %>  
			<%= link_to 'Eliminar',  client, method: :delete %>   <!--la   data: {confirm: 'deseas eleminar el usuario'}  ruta de usuario se halla mediante la primera columna en la consola: rake routs-->
             </td>
		</tr>
	<% end %>
</table>
<p><%= "Hay #{@clients.size} clientes" %></p>